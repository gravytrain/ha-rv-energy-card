{
  "name": "ha-rv-energy-card",
  "version": "0.5.0",
  "description": "Home Assistant Lovelace card: RV site power & billing overview — Meter Register design",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch"
  },
  "keywords": ["home-assistant", "lovelace", "custom-card", "energy"],
  "license": "MIT",
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
